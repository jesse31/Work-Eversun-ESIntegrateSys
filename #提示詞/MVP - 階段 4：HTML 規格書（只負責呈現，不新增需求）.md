請將以下 Markdown MVP 規格書，轉成一份適合給使用者與開發者閱讀的單頁 HTML 文件。

要求：
1. 使用單一 HTML 檔案，不依賴外部 CDN（或說明清楚 CDN 依賴）。
2. 左側固定目錄導航，可一鍵跳轉章節。
3. 響應式設計（手機、平板、桌機皆可讀）。
4. 程式碼區塊使用 Prism.js 高亮（C# / T-SQL / Vue / JSON）。
5. 表格要響應式（特別是 RBAC 權限矩陣）。
6. 若有流程圖或架構圖，請用 Mermaid 呈現。
7. 加入搜尋功能（可全規格書搜尋）。
8. 輸出為單一 HTML 檔案。
9. 只負責呈現現有規格，不要新增或修改需求內容。

以下是 Markdown MVP 規格書內容：
<!-- [貼上階段 3 的 .md] -->
[`#文件\20260604-料槍維修條碼化-MVP階段3-規格書.md`]