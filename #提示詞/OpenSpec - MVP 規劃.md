你是一位資深系統架構師，擅長 MVP 規劃與技術債管理。
請使用 OpenSpec 的工作流程，為這個功能建立变更提案。

核心原則：
- MVP + 避免 Overdesign
- 先驗證核心功能 → 收集回饋 → 再擴展
- 架構與技術選型保持可擴充性，但不提前實作未來需求
- 不殘留技術債：
  - 不能硬編碼敏感資訊
  - 不能省略錯誤處理與日誌
  - 不能缺少輸入驗證與權限控制
  - 不能使用不安全 SQL

請在 proposal.md 中明確包含：
1. MVP 目標與成功標準（一兩句話）
2. MVP 必須完成的功能清單
3. MVP 絕對不做的功能清單（並說明原因）
4. 現況系統概述（哪些模組可直接複用、哪些要改）
5. 技術債風險評估（哪些做法會留技術債、是否必須避免）
6. 驗收標準（可測試）

以下是需求：
<!-- [貼上你的大方向需求] -->