你是一位資深系統分析師與軟體架構師，擅長 MVP 與技術債管理。
請根據「現況系統分析報告（MVP 視角）」與「MVP 方案（含推薦方案）」，產出一份「MVP 规格書」，格式為 Markdown。

要求：
1. 先不要直接寫 HTML，先輸出 .md 規格書。
2. 內容需包含：
   - MVP 目標與成功標準
   - 現況系統概述（簡要）
   - 需求總覽：
     - MVP 必須完成的功能
     - MVP 絕對不做的功能（明確列出）
   - 使用者角色與權限
   - 功能需求（詳細描述）
   - 畫面流程 / 操作流程（含流程圖或 wireframe 描述）
   - 資料結構 / API（如有）
   - 與現有系統的整合方式
   - 驗收標準（可測試）
   - 風險與待確認事項
3. 技術債底線（新增章節）：
   - 列出 MVP 中「絕對不能」的做法（例如：
     - 不能硬編碼密的/鍵值
     - 不能省略錯誤處理與日誌
     - 不能缺少輸入驗證
     - 不能使用不安全 SQL（無參數化）
     - 不能缺少權限驗證
   - 列出 MVP 中「必須遵守」的技術規範（例如：
     - 必須使用參數化 SQL（Dapper / EF Core）
     - 必須有 global exception filter
     - 必須有權限驗證中間件
     - 必須有基本日誌（至少記錄錯誤與重要操作）
4. 擴充方向（'eau 未來版本）：
   - 明確標記哪些是 MVP 後才考慮的功能
   - 避免在 MVP 中提前實作
5. 輸出格式為 Markdown，使用條列與表格，內容清楚、專業、可直接交付審查。
6. 若需求不足，請先列出「待確認問題」，不要自行補太多假設。
7. 最後附上「可轉 HTML 的建議章節結構」。
8. 回覆請使用正體中文。

以下是現況系統分析報告（MVP 視角）：
[`#文件\20260604-ESIntegrateSys-現況系統分析報告.md`]

以下是 MVP 方案（含推薦方案）：
<!-- [貼上階段 2] -->
[`#文件\20260604-料槍維修條碼化-MVP階段2-方案規劃與技術債評估.md`]