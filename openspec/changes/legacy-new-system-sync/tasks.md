## 1. 準備階段

- [x] 1.1 確認 IIS 服務帳號（App Pool Identity）對 `\\192.168.4.35\EIS_Files\QS` 有讀寫權限（IIS 應用程式已設定權限帳號）
- [x] 1.2 備份 `ES_QuoteUploadFiles` 資料表（完整匯出或資料庫層級備份）（已備份整份資料庫，範圍更完整）
- [x] 1.3 備份舊系統本地路徑 `D:\...\UploadedFiles` 下所有檔案（原始檔案保留原地不動，視同備份，未刪除）
- [x] 1.4 產生遷移前的檔案清單基準（檔名、大小），供遷移後核對（以整批複製結果替代，未產生正式清單，使用者確認可接受）

## 2. 實體檔案遷移

- [x] 2.1 將 `D:\...\UploadedFiles` 目錄下所有檔案複製到 `\\192.168.4.35\EIS_Files\QS`
- [x] 2.2 核對遷移後檔案數量與檔名，與遷移前清單（任務 1.4）比對一致（已全部複製過去，確認無遺落）
- [x] 2.3 記錄並修正任何缺漏或損毀的檔案（複製過程未發現缺漏，見 2.2）

## 3. 資料庫遷移

- [x] 3.1 撰寫 SQL SELECT 語句，確認符合條件（`FilePath LIKE 'D:\%UploadedFiles%'`）的影響筆數與範圍（腳本見 `migration.sql`）
- [x] 3.2 於測試環境執行 SQL UPDATE，將 `ES_QuoteUploadFiles.FilePath` 欄位由完整路徑改為僅存檔名（已於 `ESIntegrateSysTest` 測試資料庫執行）
- [x] 3.3 驗證測試環境更新後筆數與 SELECT 結果一致，且欄位內容正確（測試資料庫 `ESIntegrateSysTest` 已驗證一致）
- [x] 3.4 於正式環境執行相同 SQL UPDATE（於任務 1.2 備份完成後）（已於正式資料庫 `ESIntegrateSys` 執行）

## 4. 程式碼修改

- [x] 4.1 於 `Web.config` 的 `<appSettings>` 新增 `FileUploadPath` 設定，值為 `\\192.168.4.35\EIS_Files\QS`
- [x] 4.2 修改 `QuoteScheduleController.Upload()`：改用 `ConfigurationManager.AppSettings["FileUploadPath"]` 組合上傳路徑，資料庫僅寫入檔名
- [x] 4.3 修改 `QuoteScheduleController.Download()`：改用設定值與檔名動態組合完整路徑後讀取檔案
- [x] 4.4 確認 `RecordDetails()` 方法邏輯不受影響（僅顯示檔名與時間，不涉及路徑組合）
- [x] 4.5 為檔案讀寫的例外處理補上明確錯誤訊息與錯誤日誌（沿用既有 try/catch + log 模式）
- [x] 4.6 `dotnet build`（或對應建置指令）確認編譯成功，無錯誤無警告（已用 MSBuild 編譯成功，QuoteScheduleController.cs 無新增警告）
- [x] 4.7 `Web.config` 的 `FileUploadPath` 比照 `connectionStrings` 既有模式，區分測試路徑（`QS_Test`）與正式路徑（`QS`，預設註解），部署正式環境前需手動切換

## 5. 驗證測試

- [x] 5.1 舊系統上傳新附件，確認檔案落於網路共享路徑（測試路徑 `QS_Test`），且資料庫 `FilePath` 僅存檔名
- [x] 5.2 舊系統下載既有（已遷移）附件，確認可正常下載且內容正確（測試路徑 `QS_Test` 已驗證）
- [x] 5.3 新系統查詢舊系統上傳的報價單，確認可看到並下載對應附件（新舊系統一上傳一下載互通已驗證）
- [x] 5.4 舊系統查詢新系統上傳的報價單，確認可看到並下載對應附件（同上，互通已驗證）
- [ ] 5.5 模擬網路路徑無法存取情境，確認系統顯示明確錯誤訊息且不崩潰（暫緩，環境無法模擬；程式碼層級的例外處理已於 4.3/4.5 實作，待日後有機會可實測）
- [x] 5.6 檢查測試路徑分離前（Task 4.7 之前）實測時是否有檔案誤寫入正式路徑 `QS`，如有需人工確認並清理（已手動清除）

## 6. 上線與收尾

- [ ] 6.1 選定離峰時段，並提前對業務/IE發布正式停機公告（含維護時段、預期影響）（待確認：是否已依此流程發布公告，或直接執行）
- [x] 6.2 執行正式環境遷移（任務 2、3），期間舊系統上傳功能暫時停用或提示維護中
- [x] 6.3 **部署前確認 `Web.config` 的 `FileUploadPath` 已從 `QS_Test` 切換為正式路徑 `QS`**（已部署正式 IIS，路徑已切換）
- [x] 6.4 上線後於正式環境重複驗證測試（任務 5.1–5.4）（新舊系統上傳/下載、含遷移前既有附件皆已於正式環境驗證通過）
- [x] 6.5 建立回滾應變：確認可透過 `Web.config` 切回本地路徑、可用備份還原 `FilePath` 欄位（機制已具備：Web.config 可切換、DB 備份見 1.2、原始檔案保留見 1.3）
- [ ] 6.6 記錄上線日期；待舊系統於新路徑正常運作滿 30 天以上，由管理者手動確認後刪除 `D:\...\UploadedFiles` 原始檔案（不建立自動清除排程）（待確認上線日期，供日後追蹤 30 天期限）
